# App Store 商店图生成器 — 产品需求文档 (PRD)

## 1. 产品定位与目标
- **定位**：一款极简、高效率的纯前端网页工具，用于快速生成符合 Apple App Store 规格的商店宣传图。
- **核心理念**：**零配置成本、固定排版布局**。用户不需要像使用 Figma 一样自由拖拽调整大小和位置，只需“选布局 -> 填文字 -> 传图片 -> 出图”。

---

## 2. 核心操作流程 (User Flow)

### 步骤 1：首页（尺寸与设备选择）
- 用户打开网页，首先选择当前要制作的设备规格：
  - **iPhone 尺寸**（默认 6.7 寸标准比例）
  - **iPad 尺寸**（默认 12.9 寸标准比例）
- 选择完毕后，点击“开始制作”，进入工作区页面。

### 步骤 2：工作区（页面列表与编辑）
- 左侧面板用于修改当前页面的各项配置，右侧是页面列表
- 列表从左至右排列，列表的最后一项就是一个+号，点击+号添加新页面。
- 页面的上方一行是页面的功能区，有【删除】【复制】【下载】按钮，下方是页面的展示区
- 列表支持拖动排序

---

## 3. 预设排版布局（Templates）
新建页面或切换布局时，提供以下固定模板（位置与比例固定，不可拖拽改变大小）：

1. **模板 A：文字在上 + 图片在下（完整显示）**
   - 上半部分为文字区，下半部分为设备截图，截图完整居中显示。
2. **模板 B：文字在上 + 图片在下（底部沉浸溢出）**
   - 上半部分为文字区，下半部分设备截图向下延伸，图片底部 20% 溢出并被页面底部裁切（营造下沉视觉）。
3. **模板 C：图片在上（完整显示） + 文字在下**
   - 上半部分显示完整设备截图，下半部分为文字区。
4. **模板 D：图片在上（顶部沉浸溢出） + 文字在下**
   - 设备截图从页面顶部向上延伸（顶部 20% 溢出裁切），下半部分为文字区。
5. **模板 E：纯图模式**
   - 无文字，整张画面居中展示设备截图。

---

## 4. 可编辑属性与约束规则

### 4.1 文字规则
- **文字字段**：包含 **标题 (Title)**。
- **对齐方式**：**强制水平居中对齐**，不允许修改对齐方式，保证排版整洁。
- **自定义项**：
  - 文字内容
  - 文字颜色
  - 字号大小（提供微调滑块或预设 大/中/小）
  - 字体选择（系统默认无衬线体、圆体等简洁字体）

### 4.2 边框与伪真机样式（框体）
- **无需复杂真机模型图**：直接对用户上传的 App 截图应用 CSS 简易边框效果。
- **边框颜色选项**：
  - 纯黑边框 (Black Frame)
  - 纯白边框 (White Frame)
  - 无边框 (No Frame)
- **边框细节**：
  - 带有适度的圆角（如 24px/32px）和极细的浅色内阴影/边线，伪装成现代化手机/平板边框。

### 4.3 背景样式
- **纯色背景**：自定义背景颜色。
- **渐变背景**（默认）：提供 5~8 组预设高颜值双色渐变（如科技蓝紫、温暖橙黄、极简深灰等），并支持自定义渐变色及角度。

---

## 5. 导出与下载功能

1. **单张下载**：
   - 每个页面右上角均有一个“下载”按钮。
   - 点击后，直接导出当前页面为高分辨率 PNG 图片（导出分辨率须严格符合所选设备的 App Store 原生导出要求，如 iPhone 导出 1290 x 2796 像素）。

---

## 6. 非功能性约束
1. **纯客户端运行**：所有图片处理、绘制、打包全在浏览器本地完成，不得依赖外部后端服务，不上传用户隐私图片。
2. **静态部署**：产物必须是纯静态网页，能直接部署在 **GitHub Pages** 上。

---

# 开发备注
- 使用技术pnpm, vue3
